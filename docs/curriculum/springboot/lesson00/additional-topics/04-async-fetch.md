# 04. 非同期通信（fetch）

## 目的
- ブラウザからAPIを呼び出す基本を身につける

## 学ぶこと
- `fetch`
- `async/await`
- 通信中/成功/失敗時の処理分岐

## 完了条件
- `GET` と `POST` のAPI呼び出しができる
- 通信失敗時にユーザーへエラー表示できる

## 対応アプリ
- `step1-greeting-web`
- `step4-reservation-form-app`

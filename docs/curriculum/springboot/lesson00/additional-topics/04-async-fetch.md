# 04. 非同期通信（fetch）

位置づけ:
- この資料は参考資料です。
- 本線では `docs/curriculum/javascript/javascript-fetch-json.md` で `fetch` / `async` / `await` を実装し、`docs/curriculum/web-app(簡易版)` でJava API連携として使います。

## 目的
- ブラウザからAPIを呼び出す基本を身につける

## 学ぶこと
- `fetch`
- `async/await`
- 通信中/成功/失敗時の処理分岐

## 完了条件
- `GET` と `POST` のAPI呼び出しができる
- 通信失敗時にユーザーへエラー表示できる

## 対応アプリ
- `step1-greeting-web`
- `step4-reservation-form-app`

## 本線教材との対応
- `fetch`: `docs/curriculum/javascript/javascript-fetch-json.md`
- `GET` / `POST` のJava API連携: `docs/curriculum/web-app(簡易版)/lesson1.md` と `lesson0.md`
- CRUDでの `PATCH` / `DELETE`: `docs/curriculum/web-app(簡易版)/lesson2.md`

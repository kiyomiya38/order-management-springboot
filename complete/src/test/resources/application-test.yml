spring:
  datasource:
    url: jdbc:h2:mem:attendance-test;MODE=MariaDB;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: false
  thymeleaf:
    cache: false

app:
  seed:
    enabled: true
    admin-password: admin123
    user-password: password

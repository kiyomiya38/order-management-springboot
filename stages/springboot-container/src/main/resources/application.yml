spring:
  application:
    name: ${APP_NAME:attendance-management}
  profiles:
    default: dev
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: ${SHOW_SQL:false}
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Tokyo
  thymeleaf:
    cache: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}
  address: ${SERVER_ADDRESS:127.0.0.1}
  servlet:
    session:
      cookie:
        # localhost上の別アプリとセッションCookieが衝突しないようにする
        name: ${SESSION_COOKIE_NAME:ATTENDANCE_CONTAINER_SESSION}
  error:
    include-message: never
    include-stacktrace: never

logging:
  level:
    root: ${LOG_LEVEL:INFO}

app:
  name: ${APP_NAME:attendance-management}
